# Abtest 표준 적용 가이드

> Multi-page A/B Test 시스템의 공식 구현 가이드

---

## 📑 목차

1. [시스템 개요](#1-시스템-개요)
2. [필수 구성 요소](#2-필수-구성-요소)
3. [표준 HTML 구조](#3-표준-html-구조)
4. [ID 명명 규칙](#4-id-명명-규칙)
5. [CSS 스타일 가이드](#5-css-스타일-가이드)
6. [JavaScript 통합](#6-javascript-통합)
7. [대시보드 사용법](#7-대시보드-사용법)
8. [API 사용 가이드](#8-api-사용-가이드)
9. [배포 체크리스트](#9-배포-체크리스트)
10. [트러블슈팅](#10-트러블슈팅)

---

## 1. 시스템 개요

### 1.1 아키텍처

```
┌─────────────────────────────────────────────────┐
│ 클라이언트 (사용자 브라우저)                      │
│ ┌─────────────────────────────────────────┐     │
│ │ HTML 페이지                               │     │
│ │ - Variant A/B 코드 포함                   │     │
│ │ - ab-test-tracker.js 로드                │     │
│ └──────────────┬──────────────────────────┘     │
└────────────────┼────────────────────────────────┘
                 │
                 ▼ GET /api/ab-test-config.php?pagePath=...
┌─────────────────────────────────────────────────┐
│ 서버 (PHP + JSON)                                │
│ ┌─────────────────────────────────────────┐     │
│ │ ab-test-config.php                      │     │
│ │ - 페이지별 설정 관리                     │     │
│ │ - GET: 설정 조회                         │     │
│ │ - POST: 설정 변경                        │     │
│ └──────────────┬──────────────────────────┘     │
│                │                                 │
│ ┌──────────────▼──────────────────────────┐     │
│ │ ab-test-config.json                     │     │
│ │ {                                        │     │
│ │   "pages": {                             │     │
│ │     "/path": { mode, variants, ... }    │     │
│ │   }                                      │     │
│ │ }                                        │     │
│ └─────────────────────────────────────────┘     │
│                                                  │
│ ┌─────────────────────────────────────────┐     │
│ │ ab-test-log.php                         │     │
│ │ - POST: 클릭 이벤트 로깅                 │     │
│ └──────────────┬──────────────────────────┘     │
│                │                                 │
│ ┌──────────────▼──────────────────────────┐     │
│ │ clicks_YYYY-MM.json                     │     │
│ │ [ { variant, elementId, ... } ]         │     │
│ └─────────────────────────────────────────┘     │
│                                                  │
│ ┌─────────────────────────────────────────┐     │
│ │ ab-test-analytics.php                   │     │
│ │ - GET: 통계 조회                         │     │
│ └─────────────────────────────────────────┘     │
└─────────────────────────────────────────────────┘
                 ▲
                 │
┌────────────────┼────────────────────────────────┐
│ 관리자 대시보드                                   │
│ ┌──────────────▼──────────────────────────┐     │
│ │ ab-test-dashboard.html                  │     │
│ │ - 페이지 선택/추가/삭제                  │     │
│ │ - 모드 설정                              │     │
│ │ - 통계 확인                              │     │
│ └─────────────────────────────────────────┘     │
└─────────────────────────────────────────────────┘
```

### 1.2 핵심 원칙

1. **페이지별 독립성**: 각 페이지는 독립적인 A/B 테스트 설정
2. **자동 감지**: 페이지 경로를 자동으로 인식
3. **쿠키 기반**: 사용자에게 일관된 경험 제공 (30일)
4. **실시간 제어**: 대시보드에서 즉시 모드 변경 가능
5. **데이터 보존**: 모든 클릭 이벤트를 JSON 파일에 영구 저장

---

## 2. 필수 구성 요소

### 2.1 파일 구조

```
프로젝트/
├── ab-test-tracker.js          # 클라이언트 스크립트 (필수)
├── ab-test-dashboard.html      # 관리자 대시보드 (필수)
└── api/
    ├── ab-test-config.php      # 설정 API (필수)
    ├── ab-test-config.json     # 설정 저장소 (자동 생성)
    ├── ab-test-log.php         # 로깅 API (필수)
    ├── ab-test-analytics.php   # 통계 API (필수)
    └── ab-test-logs/           # 로그 디렉토리 (자동 생성)
        ├── clicks_2025-11.json
        └── archive/
```

### 2.2 서버 요구사항

- **PHP**: 7.0 이상
- **JSON 확장**: 활성화 필수
- **파일 쓰기 권한**: `api/` 디렉토리
- **웹 서버**: Apache 또는 Nginx

### 2.3 브라우저 지원

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+
- 모바일 브라우저 (iOS Safari, Chrome Mobile)

---

## 3. 표준 HTML 구조

### 3.1 기본 템플릿

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>페이지 제목</title>

    <!-- 페이지 CSS -->
    <style>
        /* Variant 컨테이너는 기본적으로 숨김 */
        .dtc-dwcr-list {
            display: none;
        }
    </style>
</head>
<body>
    <!-- 일반 콘텐츠 -->
    <h1>제품 페이지</h1>

    <!-- ========================================
         A/B Test 영역 시작
         ======================================== -->

    <!-- Variant A -->
    <div class="dtc-dwcr-list" data-variant="A">
        <h2>Variant A 콘텐츠</h2>
        <a id="dtc-dwcr-action-a" href="/target-url">
            액션 버튼 A
        </a>
    </div>

    <!-- Variant B -->
    <div class="dtc-dwcr-list" data-variant="B">
        <h2>Variant B 콘텐츠</h2>
        <a id="dtc-dwcr-action-b" href="/target-url">
            액션 버튼 B
        </a>
    </div>

    <!-- ========================================
         A/B Test 영역 끝
         ======================================== -->

    <!-- AB Test 스크립트 (페이지 하단에 배치) -->
    <script src="/path/to/ab-test-tracker.js"></script>
    <script>
        window.addEventListener('DOMContentLoaded', function() {
            ABTestTracker.init();
        });
    </script>
</body>
</html>
```

### 3.2 필수 요소

#### ✅ Variant 컨테이너

```html
<div class="dtc-dwcr-list" data-variant="A">
    <!-- Variant A 전용 콘텐츠 -->
</div>

<div class="dtc-dwcr-list" data-variant="B">
    <!-- Variant B 전용 콘텐츠 -->
</div>
```

**규칙:**
- 클래스명: 반드시 `dtc-dwcr-list`
- 속성: `data-variant="A"` 또는 `data-variant="B"`
- 기본 CSS: `display: none` (tracker.js가 제어)

#### ✅ 추적 가능한 링크

```html
<a id="dtc-dwcr-{고유명}" href="...">링크 텍스트</a>
```

**규칙:**
- ID는 `dtc-dwcr-` 접두사로 시작 (필수)
- 고유명은 영문/숫자/하이픈 조합
- Variant별로 다른 ID 사용

### 3.3 잘못된 예시 ❌

```html
<!-- 잘못된 예시 1: 클래스명 오타 -->
<div class="dtc-dwcr-lists" data-variant="A">  ❌ 's' 추가됨
</div>

<!-- 잘못된 예시 2: data-variant 없음 -->
<div class="dtc-dwcr-list">  ❌ variant 지정 안됨
</div>

<!-- 잘못된 예시 3: ID 접두사 누락 -->
<a id="buy-button-a" href="...">  ❌ dtc-dwcr- 접두사 없음
</a>

<!-- 잘못된 예시 4: 중복 ID -->
<a id="dtc-dwcr-buy" href="...">A 버튼</a>
<a id="dtc-dwcr-buy" href="...">B 버튼</a>  ❌ ID 중복
```

---

## 4. ID 명명 규칙

### 4.1 표준 형식

```
dtc-dwcr-{카테고리}-{액션}-{variant}
```

**예시:**
```html
<!-- 구매 버튼 -->
<a id="dtc-dwcr-buy-now-a">Variant A 구매</a>
<a id="dtc-dwcr-buy-now-b">Variant B 구매</a>

<!-- 카트 담기 -->
<a id="dtc-dwcr-add-cart-a">A: 장바구니</a>
<a id="dtc-dwcr-add-cart-b">B: 장바구니</a>

<!-- 판매처 링크 (한맥 사례) -->
<a id="dtc-dwcr-kakao-gift">카카오 선물하기</a>
<a id="dtc-dwcr-cu-pocket">CU 포켓</a>
<a id="dtc-dwcr-gs-25">GS25</a>
```

### 4.2 카테고리별 예시

| 카테고리 | ID 예시 | 설명 |
|---------|---------|------|
| **구매** | `dtc-dwcr-buy-primary-a` | 주 구매 버튼 |
| **장바구니** | `dtc-dwcr-cart-add-a` | 장바구니 담기 |
| **위시리스트** | `dtc-dwcr-wish-add-a` | 찜하기 |
| **상세보기** | `dtc-dwcr-detail-view-a` | 상세 정보 보기 |
| **판매처** | `dtc-dwcr-{판매처명}` | 외부 판매 링크 |
| **배너** | `dtc-dwcr-banner-cta-a` | 배너 클릭 |
| **팝업** | `dtc-dwcr-popup-close-a` | 팝업 닫기 |

### 4.3 주의사항

- ✅ 소문자만 사용
- ✅ 단어 구분은 하이픈(`-`)
- ✅ 숫자 사용 가능
- ❌ 공백 사용 금지
- ❌ 특수문자 사용 금지 (하이픈 제외)
- ❌ 한글 사용 금지

---

## 5. CSS 스타일 가이드

### 5.1 기본 스타일

```css
/* 필수: Variant 컨테이너 기본값 */
.dtc-dwcr-list {
    display: none;  /* tracker.js가 제어 */
}

/* 선택사항: 표시될 때 스타일 */
.dtc-dwcr-list[style*="display: grid"],
.dtc-dwcr-list[style*="display: block"] {
    /* Variant가 활성화된 경우 추가 스타일 */
}
```

### 5.2 레이아웃 패턴

#### 그리드 레이아웃
```css
.dtc-dwcr-list {
    display: none;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
}

@media (max-width: 768px) {
    .dtc-dwcr-list {
        grid-template-columns: repeat(2, 1fr);
    }
}
```

#### Flexbox 레이아웃
```css
.dtc-dwcr-list {
    display: none;
    flex-direction: column;
    gap: 15px;
}
```

#### 세로 나열
```css
.dtc-dwcr-list {
    display: none;
    flex-direction: column;
}

.dtc-dwcr-list > * {
    margin-bottom: 20px;
}

.dtc-dwcr-list > *:last-child {
    margin-bottom: 0;
}
```

### 5.3 반응형 디자인

```css
/* 데스크톱 */
@media (min-width: 1024px) {
    .dtc-dwcr-list {
        grid-template-columns: repeat(4, 1fr);
    }
}

/* 태블릿 */
@media (min-width: 768px) and (max-width: 1023px) {
    .dtc-dwcr-list {
        grid-template-columns: repeat(3, 1fr);
    }
}

/* 모바일 */
@media (max-width: 767px) {
    .dtc-dwcr-list {
        grid-template-columns: repeat(2, 1fr);
        gap: 10px;
    }
}
```

---

## 6. JavaScript 통합

### 6.1 기본 통합

```html
<!-- 페이지 하단에 배치 -->
<script src="/path/to/ab-test-tracker.js"></script>
<script>
    window.addEventListener('DOMContentLoaded', function() {
        ABTestTracker.init();
    });
</script>
```

### 6.2 고급 옵션

#### 특정 경로만 활성화 (하위 호환용)
```javascript
// 구버전 방식 (선택사항)
ABTestTracker.init('/products/specific-product');
```

#### 이벤트 후크
```javascript
window.addEventListener('DOMContentLoaded', function() {
    ABTestTracker.init();

    // Variant 적용 완료 후 커스텀 로직
    setTimeout(function() {
        const variant = ABTestTracker.cookies.get('ab_version');
        console.log('Current Variant:', variant);

        // GA4 이벤트 전송
        if (typeof gtag !== 'undefined') {
            gtag('event', 'ab_test_view', {
                'variant': variant,
                'page_path': window.location.pathname
            });
        }
    }, 500);
});
```

#### 동적 콘텐츠 처리
```javascript
// AJAX로 콘텐츠를 로드하는 경우
function loadDynamicContent() {
    fetch('/api/content')
        .then(response => response.json())
        .then(data => {
            // 콘텐츠 삽입
            document.getElementById('dynamic-area').innerHTML = data.html;

            // AB Test 재초기화
            ABTestTracker.applyVariant();
            ABTestTracker.attachListeners();
        });
}
```

---

## 7. 대시보드 사용법

### 7.1 페이지 추가

```
1. 대시보드 접속
   → https://your-domain.com/abtest/ab-test-dashboard.html

2. "➕ 페이지 추가" 버튼 클릭

3. 정보 입력:
   - 페이지 경로: /products/your-product
   - 테스트명: 제품 구매 버튼 테스트

4. "추가" 버튼 클릭

5. 드롭다운에서 추가된 페이지 확인
```

### 7.2 모드 설정

#### 🎲 A/B 테스트 모드 (기본)
```
용도: 정상적인 50:50 테스트
설정:
1. 모드 선택: "A/B 테스트 모드"
2. "💾 설정 저장" 클릭

결과: 방문자가 랜덤하게 A 또는 B 할당
```

#### 🔒 Variant A/B 고정
```
용도: 테스트 종료 후 승자 고정, 또는 임시 긴급 대응
설정:
1. 모드 선택: "Variant A 고정" 또는 "Variant B 고정"
2. "💾 설정 저장" 클릭

결과: 모든 방문자에게 선택한 Variant만 표시
```

#### 📅 스케줄 모드
```
용도: 이벤트 기간 동안만 특별 디자인 적용
설정:
1. 모드 선택: "스케줄 모드"
2. 시작일/종료일 입력
3. 표시할 Variant 선택 (A 또는 B)
4. "💾 설정 저장" 클릭

예시:
- 시작: 2025-12-01 00:00
- 종료: 2025-12-25 23:59
- Variant: B (크리스마스 특별 디자인)

결과:
- 기간 내: 모든 방문자에게 Variant B
- 기간 외: 일반 A/B 테스트 진행
```

### 7.3 통계 확인

```
대시보드에서 자동 표시되는 정보:
- 총 클릭수
- Variant A 클릭수 및 비율
- Variant B 클릭수 및 비율
- 승자 판정 (더 많은 클릭을 받은 Variant)
- 개선율 (승자가 얼마나 더 효과적인지)
- 최근 20개 로그

자동 갱신: 30초마다
```

### 7.4 페이지 삭제

```
1. 드롭다운에서 삭제할 페이지 선택
2. "🗑️ 페이지 삭제" 버튼 클릭
3. 확인 대화상자에서 "확인"

주의: 삭제하면 되돌릴 수 없음 (로그는 보존됨)
```

---

## 8. API 사용 가이드

### 8.1 Configuration API

#### GET - 전체 페이지 조회
```bash
curl https://your-domain.com/api/ab-test-config.php
```

**응답:**
```json
{
  "pages": {
    "/products/product-1": {
      "enabled": true,
      "testName": "제품1 테스트",
      "mode": "ab_test",
      ...
    }
  },
  "global": {
    "cookieExpiry": 30,
    "defaultMode": "ab_test"
  }
}
```

#### GET - 특정 페이지 조회
```bash
curl "https://your-domain.com/api/ab-test-config.php?pagePath=/products/product-1"
```

**응답:**
```json
{
  "pagePath": "/products/product-1",
  "config": {
    "enabled": true,
    "testName": "제품1 테스트",
    "mode": "ab_test",
    "variants": { ... },
    "schedule": { ... }
  },
  "global": { ... }
}
```

#### POST - 페이지 추가
```bash
curl -X POST https://your-domain.com/api/ab-test-config.php \
  -H "Content-Type: application/json" \
  -d '{
    "action": "addPage",
    "pagePath": "/products/new-product",
    "testName": "신제품 테스트"
  }'
```

#### POST - 설정 업데이트
```bash
curl -X POST https://your-domain.com/api/ab-test-config.php \
  -H "Content-Type: application/json" \
  -d '{
    "action": "update",
    "pagePath": "/products/product-1",
    "mode": "force_a",
    "updatedBy": "admin"
  }'
```

#### POST - 페이지 삭제
```bash
curl -X POST https://your-domain.com/api/ab-test-config.php \
  -H "Content-Type: application/json" \
  -d '{
    "action": "deletePage",
    "pagePath": "/products/product-1"
  }'
```

### 8.2 Analytics API

```bash
curl "https://your-domain.com/api/ab-test-analytics.php?\
startDate=2025-11-01&\
endDate=2025-11-15&\
pagePath=/products/product-1"
```

**응답:**
```json
{
  "summary": {
    "totalClicks": 150,
    "variantA": 75,
    "variantB": 75,
    "winner": "동점",
    "improvement": 0
  },
  "variants": {
    "A": {
      "total": 75,
      "channels": {
        "dtc-dwcr-buy-a": 45,
        "dtc-dwcr-cart-a": 30
      }
    },
    "B": { ... }
  },
  "logs": [ ... ]
}
```

#### CSV 내보내기
```bash
curl "https://your-domain.com/api/ab-test-analytics.php?\
startDate=2025-11-01&\
endDate=2025-11-15&\
export=csv" > report.csv
```

---

## 9. 배포 체크리스트

### 9.1 파일 업로드

```
□ ab-test-tracker.js → /js/ 또는 /assets/js/
□ ab-test-dashboard.html → /abtest/ 또는 /admin/
□ ab-test-config.php → /api/
□ ab-test-log.php → /api/
□ ab-test-analytics.php → /api/
```

### 9.2 디렉토리 권한

```bash
# 로그 디렉토리 생성 및 권한 설정
mkdir -p /path/to/api/ab-test-logs
chmod 755 /path/to/api/ab-test-logs

# Apache 사용자에게 쓰기 권한
chown apache:apache /path/to/api/ab-test-logs

# 또는 nginx
chown nginx:nginx /path/to/api/ab-test-logs
```

### 9.3 서버 설정 확인

```bash
# PHP 버전 확인
php -v

# JSON 확장 확인
php -m | grep json

# 파일 쓰기 테스트
touch /path/to/api/test.txt
rm /path/to/api/test.txt
```

### 9.4 HTML 페이지 수정

```
□ Variant A/B 코드 삽입
□ ID 명명 규칙 준수 확인
□ ab-test-tracker.js 스크립트 추가
□ init() 호출 확인
```

### 9.5 대시보드 설정

```
□ 페이지 추가
□ 테스트명 입력
□ 모드 선택 (기본: A/B 테스트)
□ 설정 저장
```

### 9.6 테스트

```
□ 브라우저에서 페이지 접속
□ 개발자 도구 콘솔 확인 (에러 없는지)
□ 쿠키 확인 (ab_version=A 또는 B)
□ Variant A/B 번갈아 표시 확인 (쿠키 삭제 후)
□ 클릭 이벤트 발생
□ Analytics API에서 로그 확인
```

---

## 10. 트러블슈팅

### 10.1 Variant가 표시되지 않음

**증상:**
- 페이지에 아무것도 표시되지 않음
- 두 Variant 모두 숨김

**원인 및 해결:**

1. **클래스명 오타**
   ```html
   ❌ <div class="dtc-dwcr-lists" data-variant="A">
   ✅ <div class="dtc-dwcr-list" data-variant="A">
   ```

2. **CSS display:none 강제 적용**
   ```css
   ❌ .dtc-dwcr-list { display: none !important; }
   ✅ .dtc-dwcr-list { display: none; }
   ```

3. **스크립트 로드 실패**
   ```html
   <!-- 개발자 도구에서 404 에러 확인 -->
   ✅ 정확한 경로로 수정
   ```

4. **페이지가 대시보드에 등록되지 않음**
   ```
   → 대시보드에서 페이지 추가
   → enabled: true 확인
   ```

### 10.2 클릭이 로깅되지 않음

**증상:**
- Analytics에 데이터 없음
- clicks_YYYY-MM.json 파일 생성 안됨

**원인 및 해결:**

1. **ID 접두사 누락**
   ```html
   ❌ <a id="buy-button" href="...">
   ✅ <a id="dtc-dwcr-buy-button" href="...">
   ```

2. **로그 디렉토리 권한 문제**
   ```bash
   # 권한 확인
   ls -la /path/to/api/ab-test-logs

   # 권한 수정
   chmod 755 /path/to/api/ab-test-logs
   chown apache:apache /path/to/api/ab-test-logs
   ```

3. **PHP 에러**
   ```bash
   # 에러 로그 확인
   tail -f /var/log/httpd/error_log
   # 또는
   tail -f /var/log/nginx/error.log
   ```

4. **CORS 문제**
   ```php
   // ab-test-log.php 상단에 추가
   header('Access-Control-Allow-Origin: *');
   ```

### 10.3 통계가 갱신되지 않음

**증상:**
- 대시보드에서 클릭수 0으로 표시
- 로그 파일은 생성됨

**원인 및 해결:**

1. **날짜 범위 확인**
   ```
   대시보드는 기본적으로 최근 30일 조회
   → 날짜 필터 조정
   ```

2. **페이지 경로 불일치**
   ```
   로그의 pagePath와 대시보드의 선택 페이지 비교
   → 정확히 일치해야 함
   ```

3. **JSON 파싱 에러**
   ```bash
   # JSON 유효성 검사
   cat /path/to/api/ab-test-logs/clicks_2025-11.json | python -m json.tool
   ```

### 10.4 설정이 적용되지 않음

**증상:**
- 대시보드에서 설정 저장 후에도 변경 안됨
- 여전히 이전 모드로 동작

**원인 및 해결:**

1. **브라우저 캐시**
   ```
   → Ctrl+Shift+R (강제 새로고침)
   → 시크릿 모드에서 테스트
   ```

2. **쿠키 영향**
   ```
   이전 쿠키가 남아있으면 기존 Variant 유지
   → 쿠키 삭제 후 재테스트
   ```

3. **설정 파일 권한**
   ```bash
   chmod 666 /path/to/api/ab-test-config.json
   ```

### 10.5 개발자 도구 활용

```javascript
// 콘솔에서 현재 상태 확인
console.log('현재 Variant:', ABTestTracker.cookies.get('ab_version'));
console.log('서버 설정:', ABTestTracker.serverConfig);

// 강제로 Variant 변경 (테스트용)
ABTestTracker.cookies.set('ab_version', 'B', 30);
location.reload();
```

---

## 📚 관련 문서

- [Abtest 실제 적용 예제 모음](./Abtest_실제_적용_예제_모음.md)
- [API Reference](./API_Reference.md)
- [FAQ](./FAQ.md)

---

## 🆘 지원

문제가 해결되지 않으면:
1. 개발자 도구 콘솔 스크린샷
2. 에러 로그
3. HTML 코드 일부

를 포함하여 이슈 등록

---

**작성일:** 2025-11-15
**버전:** 2.0 (Multi-page 지원)
**최종 업데이트:** 2025-11-15
